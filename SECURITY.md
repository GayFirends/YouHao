# 安全策略

## 支持范围

油迹目前处于早期开发阶段。安全修复优先应用到默认分支和最新发布版本，不承诺维护更早的预发布版本。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的漏洞、利用方式、真实凭据或用户数据。

优先通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能提交，并包含：

- 受影响版本和平台；
- 可复现的最小步骤；
- 可能造成的影响；
- 建议的修复方向（如有）。

维护者确认问题前，请避免公开漏洞细节。修复发布后会在 CHANGELOG 和 GitHub Security Advisory 中说明影响范围。

## 数据安全边界

- WebDAV 同步文件默认未加密，应只通过可信 HTTPS 服务传输。
- WebDAV 密码不会写入同步文件，但仍属于敏感信息。
- JSON 备份可能包含车辆、里程、加油地点和备注，应作为私人数据妥善保存。
- 本项目不会要求用户在 Issue 中上传真实数据库或完整备份。
