# 参与贡献

感谢你愿意帮助改进油迹。提交代码前，请先搜索现有 Issue，避免重复工作；较大的功能建议先创建 Issue 讨论范围和数据兼容性。

## 本地开发

需要 Node.js 20+、npm；构建 Android 还需要 Java 21 和 Android SDK。

```bash
npm install
npm run dev
```

提交前运行：

```bash
npm test
npm run build
npm run test:e2e
```

## 改动原则

- 保持本地优先：断网时仍能完成核心记录与查询。
- 修改数据库结构时提供版本化迁移，并覆盖旧数据升级测试。
- 修改备份或同步格式时保持旧备份可导入。
- 同步、删除和冲突处理必须考虑多设备与失败重试。
- UI 改动同时检查桌面端、390px 手机和无障碍键盘操作。
- 不在 Issue、日志、测试夹具或截图中提交真实账号、密码、车牌和 WebDAV 地址。

## Pull Request

请在 PR 中说明：

1. 解决的问题和用户可见变化；
2. 影响的平台（Web、Android 或两者）；
3. 数据迁移、同步和备份兼容性；
4. 已执行的测试；
5. UI 改动的桌面与移动截图。

小而聚焦的 PR 更容易审查。不要将无关格式化、依赖升级和功能改动混在同一个提交中。
