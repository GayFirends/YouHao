# Changelog

本文件记录油迹项目的重要功能和行为变更。

## [Unreleased]

## [0.2.0] - 2026-09-18

### 产品化基础

- SQLite schema 升级到 v5，新增查询索引、安全快照、恢复、分页查询、车辆摘要、软删除清理和冲突版本表。
- 新增多设备确认水位、90 天安全墓碑清理和本机／云端／手动冲突解决界面。
- 同步基线和设备确认缓存按 WebDAV 地址与文件名隔离，切换同步目标不会复用旧服务器状态。
- Android 可使用 Keystore 安全记忆同步口令，并在 API 23、35 模拟器运行 instrumentation 测试。
- WebDAV 支持可选的 v2 加密容器：PBKDF2-SHA-256、AES-256-GCM 和 gzip，同时继续兼容 v1 明文备份。
- 增加统一错误码、启动失败救援导出、脱敏诊断包、PWA 外壳以及同步口令迁移保护。
- 车辆与同步设置从应用壳拆分，账本按 50 条渐进渲染，并增加 1 万条记录性能基准。
- 加入 ESLint、Prettier、80% 覆盖率门槛、依赖审计、构建体积预算和 Dependabot。
- Playwright 同时覆盖桌面和移动 Chromium，并加入严重级无障碍扫描。
- Android Release 开启 R8 和资源压缩，Debug 与 Release APK 构建通过。

### 新增

- 新增首次使用三步引导，说明车辆、满箱油耗和本地优先的数据方式。
- 新增新版 App Icon、Android 自适应图标与深松绿启动屏。
- 新增 Playwright 关键用户流程测试。
- 新增 MIT License、贡献指南、安全策略和 GitHub Issue 模板。

- 加油记录支持填写“加油机表显金额”。
- 加油记录支持手动填写“加油机表显单价”，单价保留两位小数。
- 新增加油信息中显示：
  - 表显单价
  - 优惠后单价
  - 优惠金额
- 加油金额支持双向反算，任意填写以下两项即可计算第三项：
  - 加油量
  - 加油机表显金额
  - 加油机表显单价
- 实付金额和加油量变化时，优惠后单价和优惠金额会实时更新。
- CSV 导出新增表显金额、优惠金额、表显单价和优惠后单价字段。

### 界面

- 采用「松间仪表盘」方案：深松绿品牌区、荧光黄绿关键状态、浅灰绿背景与更鲜明的数据层级。
- 首页优先展示平均油耗，增加可选择区间的油耗趋势图，同时保留本月油费、记录里程和最近加油。
- 加油表单优先展示常用字段，优惠信息可按需展开，保存按钮在滚动时保持可见。
- 加油记录按月份分组，支持关键词搜索、月份筛选和实付／优惠合计；桌面使用表格，手机使用账本列表。
- 手机底部导航增加「记一笔」入口，车辆和数据页面沿用纸本配色。
- 弹窗支持 Escape、键盘焦点管理和保存中防重复提交；错误提示显示在保存按钮上方。

### 修复

- Web 保存改为事务内检查快照版本并重试，避免多个页面新增记录时整库相互覆盖。
- Web 写入失败时丢弃未提交的数据库副本，避免失败记录进入后续导出或保存。
- 重新开启 SQLite 导出后重置的外键检查；本地数据库操作串行执行，避免 Android 事务重叠。
- 表单重试沿用同一记录 ID 和创建时间，切回页面时刷新本地数据。
- 同步比较实际时间并固定字段顺序；相同更新时间优先保留删除标记，导入时间统一为 UTC 格式。
- 普通加油不再要求填写表显信息，默认使用实付金额；编辑优惠记录时保留原表显金额。
- 金额联动计算与保存状态变化时，保留已填写的日期、里程、车辆和备注。
- 跨车辆录入时，异常提示与表单所选车辆的记录比较。
- 初始里程为 0 时正确累计里程，手机上的单个油耗区间也能显示。

### 数据与兼容性

- 本地 SQLite 数据库升级到 v3，新增 `pumpAmount` 字段。
- Web 和 Android 数据库均支持从旧版本自动迁移。
- 旧记录迁移时，表显金额默认使用原实付金额，按无优惠记录处理。
- 旧版 JSON 备份中没有表显金额时，导入后自动使用实付金额填充。

### 测试

- 新增真实 SQLite WASM 配合 IndexedDB 模拟环境的回归测试，覆盖多页面写入、失败回滚、外键约束、旧库迁移与原子合并。
- 新增数据库操作排队、时区、相同时间戳和字段顺序的回归测试。
- 新增优惠金额、优惠后单价和表显单价计算测试。
- 新增旧版 JSON 备份兼容性测试。
- 新增普通加油、优惠保留、反算加油量、手动单价和零实付金额的测试。
- 单元与数据库回归测试扩展到 96 项，覆盖三方冲突识别、冲突持久化与解决、设备水位条件写入、目标隔离及 Keystore 桥接。
- 生产构建验证通过。
- npm 安全审计无已知漏洞。
- 浏览器验证覆盖 320、390、768、1024、1440 像素布局，以及新增、编辑、删除、筛选、JSON 导出、重载持久化和保存失败提示。
- Android 页面资源同步通过，尚未完成 Android 真机验证。
